<?xml version="1.0" encoding="UTF-8"?>
<config>
    <!-- These are the standard packages that are white-listed to always have internet access while in power save mode, even if they aren't in the foreground. -->
    <!-- SmartSwich should not enter the power saving state due to the backup and restoration key function. -->
    <allow-in-power-save package="com.sec.android.easyMover" />
</config>
